
  # EventSquid – SiteBuilder Prototype

  This is a code bundle for EventSquid – SiteBuilder Prototype. The original project is available at https://www.figma.com/design/i4eWgqiJ3GseYcG8rur46Y/EventSquid-%E2%80%93-SiteBuilder-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  